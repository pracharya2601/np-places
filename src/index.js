const { state } = require('./state');
const { districts, districtList } = require('./district');

const mainExport = {
  state,
  districts,
  districtList,
};
/* output item */
module.exports = mainExport;
