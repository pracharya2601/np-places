const { state } = require('./state');
const { districts, districtList } = require('./district');

const npPlaces = {
  state,
  districts,
  districtList,
};
/* output item */
module.exports = npPlaces;
